---
interface Props {
  title: string;
  image: string;
}

const { title, image } = Astro.props;
---

<div class="reveal flex flex-col gap-[24px] items-start">
  <div class="h-[200px] md:h-[300px] w-full overflow-hidden border border-border-secondary bg-[var(--card-bg)]">
    <img
      src={image}
      alt={title}
      class="size-full object-cover"
      loading="lazy"
    />
  </div>
  <h3 class="text-[16px] font-semibold leading-[24px] tracking-[-0.5px] text-content-primary">
    {title}
  </h3>
</div>
