---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col gap-s items-start">
  <div class="h-[5px] w-[18px] bg-content-primary"></div>
  <span class="font-mono text-[12px] font-semibold uppercase text-content-tertiary">
    {title}
  </span>
</div>
